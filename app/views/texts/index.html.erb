<h3>テキスト一覧ページ</h3>
<% @texts.each do |text| %>
<div class="card">
  <div class="card-body">
    <h5 class="card-title"><%= text.title %></h5>
    <p class="card-text"><%= text.genre %></p>
    <%= link_to "詳細ページ", text_path(text) %>
  </div>
</div>
<% end %>